<nav class="navbar">
    <div class="nav-container">
        <div class="nav-brand">
            <a href="/">
                <% if (user && user.avatar) { %>
                    <img src="https://cdn.discordapp.com/avatars/<%= user.discord_id %>/<%= user.avatar %>.png?size=32" 
                         alt="Profile" class="nav-avatar">
                <% } %>
                <span>About Me</span>
            </a>
        </div>
        
        <div class="nav-menu">
            <a href="/" class="nav-link">Home</a>
            <a href="/projects" class="nav-link">Projects</a>
            
            <% if (user) { %>
                <% if (isAuthorized) { %>
                    <a href="/admin" class="nav-link">Admin</a>
                <% } %>
                <a href="/auth/logout" class="nav-link logout">Logout</a>
            <% } else { %>
                <% if (isAuthorizedIP) { %>
                    <a href="/auth/login" class="nav-link login">Login with Discord</a>
                <% } %>
            <% } %>
        </div>
        
        <div class="nav-toggle">
            <span></span>
            <span></span>
            <span></span>
        </div>
    </div>
</nav>
